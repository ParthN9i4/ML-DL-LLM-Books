# Encrypted-LLM paper comparison table

One row per paper, columns 1–7 of the nine-point template in `study-plan.html`.
Points 8 (the claim to attack) and 9 (consequence for the OpenFHE pipeline) live in the
per-paper analysis files — point 8 is Parth's alone and is never machine-drafted.

"n/r" = not reported by the paper. The gaps are data: note where they cluster.

| # | Paper | Threat model | Scheme + params | Mult. depth | Non-poly handling (softmax / norm / activation) | Packing & slot use | Benchmark (model, seq, hw, latency, exclusions) | Accuracy delta |
|---|-------|--------------|------------------|-------------|--------------------------------------------------|--------------------|--------------------------------------------------|----------------|

<!-- The weekly Routine appends one row per paper below this line. Rows are drafts
     until Parth has read the paper and either confirmed or corrected each cell. -->
